/* SHIELDTECH — Decap CMS bootstrap.
 *
 * The CDN script (decap-cms.js) auto-initializes from admin/config.yml.
 * This file is a hook for any custom widget registrations before init.
 * If you add custom widgets, register them here, then call:
 *     window.CMS.registerWidget(...);
 * (The default widgets cover products + settings already.)
 */
if (window.CMS) {
  /* Example — correct EXIF orientation of uploaded images is handled by
     Decap automatically; nothing extra needed here. */
}
